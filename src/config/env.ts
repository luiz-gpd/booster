import * as dotenv from 'dotenv'
dotenv.config()

const {
  PORT,
  INCOMING_LECTURE_CRON,
  SYSTEM_TIMEZONE,
  PRIVATE_KEY_BASE64,
  PUBLIC_KEY_BASE64,
  DB_HOST,
  DB_USERNAME,
  DB_NAME,
  DB_PASSWORD,
  DB_PORT,
} = process.env;

export {
  PORT,
  INCOMING_LECTURE_CRON,
  SYSTEM_TIMEZONE,
  PRIVATE_KEY_BASE64,
  PUBLIC_KEY_BASE64,
  DB_HOST,
  DB_USERNAME,
  DB_NAME,
  DB_PASSWORD,
  DB_PORT,
};
